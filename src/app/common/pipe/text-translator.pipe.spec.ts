import { TextTranslatorPipe } from './text-translator.pipe';

describe('TextTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TextTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
